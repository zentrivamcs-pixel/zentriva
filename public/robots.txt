# https://www.robotstxt.org/robotstxt.html
# NOTE: the deployed robots.txt is regenerated at build time by
# scripts/build-seo.js from src/shared/seo.json, so the Sitemap line always
# carries the right origin. Edit it there — this copy only serves `npm start`.

User-agent: *
Allow: /
Disallow: /admin
Disallow: /member

Sitemap: https://zentrivacoop.com/sitemap.xml
